# MovieRatingProject

The approach I handled with the MovieRatingSystem java file is splitting up the parts into different sections before connecting them altogether. The Scanner user input for one used to be multiple and separate inputs for the name, movies, genre, and so on. Only issue was closing the Scanner inputs, and I have noticed they were added code when I can simplify it to one Scanner input for all.

For-while statement used for validating movie ratings.

Switch statement is established in Step 7, and relies on the cases of the following strings enabled for each response.

Used conditional operator for horror recommendation if the input aligns with "horror" as the favorite genre.